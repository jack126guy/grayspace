---
import BasePage from '../components/BasePage.astro';
import siteInfo from 'virtual:grayspace/site-info';
import { translate, type TextDirection } from '../i18n';
import 'virtual:grayspace/site-styles';

interface Props {
	title: string;
}

const currentLocale = Astro.currentLocale;
const translations = translate(currentLocale);
const { title } = Astro.props;
const { favicon } = siteInfo;
const dir = translations.__('ltr') as TextDirection;
const skipToMainText = translations.__('Skip to main content');
---

<BasePage
	lang={currentLocale}
	dir={dir}
	favicon={favicon}
	title={title}
	skipToMainText={skipToMainText}
>
	<slot slot="head" name="head" />
	<slot slot="header" name="header" />
	<slot />
	<slot slot="footer" name="footer" />
</BasePage>
